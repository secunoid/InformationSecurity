# Kali Linux
Tag: #kali #linux
- Upgrade Kali: *sudo  apt update && apt -y full-upgrade*
- Install ssh:
	- sudo apt install ssh
	- sudo systemctl start ssh.service
	- sudo systemctl enable ssh.servicw
- MurliDharBhootnika.1963!